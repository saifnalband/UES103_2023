#include <stdio.h>
int main(){
    void disp( char ch);
    char ch;
    char arr[] = {'a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i', 'j'};
    for (int x=0; x<10; x++){
        printf("%d ", ch);
    }
    return 0;
}
